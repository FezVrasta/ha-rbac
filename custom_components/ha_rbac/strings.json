{
  "config": {
    "step": {
      "user": {
        "title": "RBAC access control",
        "description": "This answers on the port your browsers already use and forwards to Home Assistant, with each user's role applied on the way through.\n\nThe defaults suit most installations.\n\n{warning}",
        "data": {
          "proxy_port": "Proxy port",
          "bind_address": "Bind address",
          "upstream_host": "Home Assistant host",
          "upstream_port": "Home Assistant port"
        },
        "data_description": {
          "proxy_port": "The port browsers connect to: the one Home Assistant answers on today, so nothing you already use has to be repointed.",
          "bind_address": "Which network to answer on. 0.0.0.0 means all of them.",
          "upstream_host": "Where Home Assistant itself listens. Leave this as 127.0.0.1.",
          "upstream_port": "Where Home Assistant will answer instead. Nothing you use needs to know about this port."
        }
      },
      "move": {
        "title": "Move Home Assistant out of the way",
        "description": "Roles are only enforced on traffic that comes through here, so Home Assistant has to stop answering on the network itself. Otherwise anyone can go straight round this with the token they already have.\n\nHome Assistant can be moved to port {upstream_port}, reachable only from its own machine, leaving port {proxy_port} to this. It restarts once and comes back on {proxy_port} in about fifteen seconds.\n\nIf it does not come back, Home Assistant undoes the change within five minutes and restarts again on {proxy_port} as it is now.",
        "data": {
          "manage_http": "Move Home Assistant for me"
        },
        "data_description": {
          "manage_http": "Leave this off to do it yourself under Settings > System > Network. Nothing is enforced until it is done."
        }
      }
    },
    "error": {
      "port_conflict": "The proxy cannot listen on the same port as Home Assistant."
    },
    "abort": {
      "already_configured": "Access control is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "RBAC access control",
        "data": {
          "proxy_port": "Proxy port",
          "bind_address": "Bind address",
          "upstream_host": "Home Assistant host",
          "upstream_port": "Home Assistant port",
          "manage_http": "Keep Home Assistant out of the way",
          "restore_network_on_removal": "Put Home Assistant back if this is removed"
        },
        "data_description": {
          "proxy_port": "The port browsers connect to: the one Home Assistant answers on today, so nothing you already use has to be repointed.",
          "bind_address": "Which network to answer on. 0.0.0.0 means all of them.",
          "upstream_host": "Where Home Assistant itself listens. Leave this as 127.0.0.1.",
          "upstream_port": "Where Home Assistant will answer instead. Nothing you use needs to know about this port.",
          "manage_http": "Move Home Assistant to the port above, reachable only from its own machine, restarting it if it is not there already.",
          "restore_network_on_removal": "Removing or disabling this integration moves Home Assistant back to the address it answered on before, and restarts it once. Leave this off and it stays where it is, reachable only from its own machine."
        }
      }
    }
  }
}
