[tool.ruff]
target-version = "py313"
line-length = 88
# Ruff formats JSON now, and writes a trailing comma before the closing brace
# -- valid JSON5, not valid JSON. Home Assistant reads `strings.json` and
# `translations/*.json` with a strict parser, so a formatted one is a file the
# integration can no longer load.
#
# It only happens when a JSON file is named directly: `ruff format .` walks for
# Python and never reaches them, which is why CI has always been green. An
# editor formatting the file you have open does name it directly, and so does
# `ruff format <changed file>`, so the corruption arrives locally and silently.
#
# `force-exclude` is the half that matters. Without it an exclude is ignored
# for a path given explicitly, which is exactly the case being guarded, and the
# setting reads as working while changing nothing.
extend-exclude = ["*.json"]
force-exclude = true

# Mirrors the Home Assistant core ruff configuration, so the component matches
# the conventions of the codebase it plugs into.
[tool.ruff.lint]
select = ["A001", "ASYNC", "B", "BLE", "C", "COM818", "D", "DTZ003", "DTZ004", "DTZ011", "E", "F", "F541", "FLY", "FURB", "G", "I", "INP", "ISC", "ICN001", "LOG", "N804", "N805", "N806", "N815", "PERF", "PGH", "PIE", "PL", "PT", "PTH", "PYI", "RET", "RSE", "RUF", "S107", "S102", "S103", "S108", "S301", "S306", "S307", "S313", "S314", "S315", "S316", "S317", "S318", "S319", "S601", "S602", "S604", "S608", "S609", "SIM", "SLF", "SLOT", "T100", "T20", "TC", "TID", "TRY", "UP", "UP031", "UP032", "W"]
ignore = ["ASYNC109", "ASYNC110", "ASYNC240", "B008", "B019", "D202", "D203", "D213", "D406", "D407", "D417", "E501", "PLC1901", "PLR0911", "PLR0912", "PLR0913", "PLR0915", "PLR0917", "PLR2004", "PLW0108", "PLW1641", "PLW2901", "PT011", "PT018", "RUF001", "RUF012", "RUF015", "RUF043", "SIM102", "SIM103", "SIM108", "SIM115", "TC001", "TC002", "TC003", "TC006", "TRY003", "TRY400", "UP047", "UP049", "W191", "E111", "E114", "E117", "D206", "D300", "Q", "COM812", "COM819", "PLE0605", "FURB116", "ISC004", "LOG004"]

[tool.ruff.lint.per-file-ignores]
# The suite reuses Home Assistant core's `tests` package for its fixtures, so
# this directory deliberately is not an importable package of its own.
"tests/*" = ["SLF001", "PLR2004", "INP001"]
"testsupport/*" = ["INP001"]

[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false

[tool.ruff.lint.pydocstyle]
convention = "pep257"

[tool.ruff.lint.mccabe]
max-complexity = 25
